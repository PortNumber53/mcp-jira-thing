# Backend HTTP server
BACKEND_ADDR=:18111

# Primary (non-Xata) database used by the backend at runtime.
# This should point at your new Postgres instance.
DATABASE_URL=postgresql://user:password@localhost:5432/mcp_jira_thing?sslmode=disable

# Legacy Xata database (temporary, used only for migration/sync on startup).
#
# Preferred: provide the full Xata Postgres endpoint DSN directly.
# XATA_DATABASE_URL=postgresql://workspace:<API_KEY>@us-east-1.sql.xata.sh/dbjirathing:main?sslmode=require
#
# Alternatively: provide pieces and the backend will construct XATA_DATABASE_URL.
XATA_API_KEY=
XATA_WORKSPACE=
XATA_DATABASE=
XATA_BRANCH=main
XATA_REGION=us-east-1


