BACKEND_ADDR=:18003
XATA_API_KEY=changeme
XATA_WORKSPACE=your-workspace
XATA_DATABASE=dbjirathing
XATA_BRANCH=main
XATA_REGION=us-east-1
# Optional override for outbound request timeout (seconds)
# BACKEND_HTTP_TIMEOUT_SECONDS=15
# Alternatively, you can supply DATABASE_URL instead of the individual values:
# DATABASE_URL=postgresql://workspace:<API_KEY>@us-east-1.sql.xata.sh/dbjirathing:main?sslmode=require
